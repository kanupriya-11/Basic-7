#include <iostream>
using namespace std;

int main() {
   int n,a=0,b=1,num;
   cout<<"Enter the number: ";
   cin>>n;
   
   cout<<"0,"<<"1";
   for(int i=2;i<n;i++)
   {
       num=a+b;
       a=b;
       b=num;
       cout<<","<<num;
   }
    return 0;
}
